import { client } from "@/sanity/lib/client";
import { ReferenceInput } from "sanity";

ReferenceInput


const ProductCards = () => {
   client
    returs (
        <div>
            <h2>Products</h2>
        </div>
    );
};

export default ProductsCards